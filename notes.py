# strip removes spaces in the beginning or int the end of the string
# text.format(age,height,weight..) (used while printing)
# \ is the escape character
# \r is carriage return and \b is backspace
# .extend(anotherList)

# List -> ordered,changable,duplicates allowed
# Tuple -> ordered,unchangable,duplicates allowed
# Set -> unordered,unchangable,duplicates not allowed
# Dict -> ordered,changable,duplicates not allowed

# del thisList and thisList.clear() are different
# List comprehension -> newList = [x for x in range(10) if x<5]
# newList = [x if x!=3 else 5 for x in range(10)]
# .sort(key = myfunc) .sort(reverse = True) .sort(key = str.lower)
# myList = list(thisList) or use .copy()

# unpack tuples by (a,b,c)=fruits

# set(("banana","apple","orange")) same as {"banana","apple","orange"}
# use discard instead of remove
# set1.union(set2) x.union_update(y) symmetric_difference()

# keys values and items
# .update({"year"="2023"})


# a = list(map(int,input().split()))


# file=open('text.txt','a')
# file.write("my lvda")
# file.close()
# file.write("\nthis is my command\nf#^(#&) you!")
# file.close()

# with open('text.txt') as f:
#     data = f.readlines()
# for i in data:
#     words=i.split()
#     print(i)


